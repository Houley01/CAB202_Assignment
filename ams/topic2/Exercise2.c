#include <stdio.h>

void count_down(void)
{
    for(int count = 3457; 76 < count; count= count-8)
    {
        printf("%d\n", count);
    }
    
}

int main(void)
{
    count_down();
    return 0;
}
